<h1>Add a new movie</h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.submit "Submit movie", class: "btn btn-primary" %>
<% end %>
</br>
<p><%= link_to "Go back to #{@list.name} list", list_path(@list) %></p>
